[capture]
   path = {{ deployer.paths.capture }}
   browseable = yes
   read only = no
   guest ok = yes
   force user = nobody

[deploy]
   path = {{ deployer.paths.root }}
   browseable = yes
   read only = no
   guest ok = no
   valid users = {{ deployer.samba.username }}
   force user = nobody

[join]
   path = {{ deployer.paths.join_secret }}
   browseable = no
   read only = yes
   guest ok = no
   valid users = {{ deployer.samba.username }}
   force user = root
