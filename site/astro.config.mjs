// @ts-check
import { readFileSync } from 'node:fs';
import { defineConfig } from 'astro/config';
import starlight from '@astrojs/starlight';
import starlightThemeRapide from 'starlight-theme-rapide';

// Generated by scripts/sync-content.mjs (run via `npm run sync` / `npm run build`).
const sidebar = JSON.parse(readFileSync(new URL('./.generated/sidebar.json', import.meta.url), 'utf8'));

// https://astro.build/config
export default defineConfig({
	site: 'https://grantcurell.com',
	integrations: [
		starlight({
			plugins: [starlightThemeRapide()],
			title: "Grant Curell's Projects",
			description:
				'Notes and walkthroughs from every time I had to figure something out: networking, storage, servers, Linux internals, and more.',
			social: [
				{
					icon: 'github',
					label: 'GitHub',
					href: 'https://github.com/grantcurell/projects',
				},
			],
			head: [
				{
					tag: 'meta',
					attrs: { property: 'og:image', content: 'https://grantcurell.com/og-default.png' },
				},
				{
					tag: 'meta',
					attrs: { name: 'twitter:card', content: 'summary_large_image' },
				},
				{
					tag: 'meta',
					attrs: { name: 'twitter:image', content: 'https://grantcurell.com/og-default.png' },
				},
			],
			sidebar,
			pagination: false,
			expressiveCode: {
				shiki: {
					// Language tags used in the notes that shiki doesn't know natively.
					// "asm" covers the assembly blocks; C is the closest grammar for
					// device tree source (colors comments/strings/numbers correctly).
					langAlias: {
						assembly: 'asm',
						dts: 'c',
					},
				},
			},
		}),
	],
});
