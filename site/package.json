{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "sync": "node scripts/sync-content.mjs",
    "dev": "npm run sync && astro dev",
    "start": "npm run dev",
    "build": "npm run sync && astro build && node scripts/emit-redirects.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.40.0",
    "astro": "^6.4.5",
    "sharp": "^0.34.5",
    "starlight-theme-rapide": "^0.5.2"
  }
}
